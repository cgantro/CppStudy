enable_testing()

# Google Test (optional) — requires vcpkg or FetchContent
# find_package(GTest CONFIG QUIET)
# if(GTest_FOUND)
#   add_executable(my_test my_test.cpp)
#   target_link_libraries(my_test PRIVATE GTest::gtest_main)
#   gtest_discover_tests(my_test)
# endif()
