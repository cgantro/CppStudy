# Google Benchmark (optional) — requires vcpkg or FetchContent
# find_package(benchmark CONFIG QUIET)
# if(benchmark_FOUND)
#   add_executable(bench_example bench_example.cpp)
#   target_link_libraries(bench_example PRIVATE benchmark::benchmark_main)
# endif()
