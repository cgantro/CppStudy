## 함수 타입, 함수 포인터, 함수 객체

## [코드](./code/function.cpp)

함수 자체는 객체가 아니다.

- 함수 포인터는 객체다

현대 c++에서는 함수 포인터보다 함수 객체, 람다, `std::function`을 사용한다.

람다는 익명 클래스 객체에 가깝다.
```cpp
int base = 10;
auto addBase = [base](int x){
    return base + x;
};

```